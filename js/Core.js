const Core = (() => {
    const operations = {
        spaceToTab: "space-to-tab",
        tabToSpace: "tab-to-space",
    };

    return {
        operations: operations,
    };
})();

export default Core;